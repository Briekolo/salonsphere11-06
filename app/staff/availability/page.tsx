import { StaffAvailabilityManager } from '@/components/staff/availability/StaffAvailabilityManager';

export default function StaffAvailabilityPage() {
  return <StaffAvailabilityManager />;
}

export const metadata = {
  title: 'Beschikbaarheid - SalonSphere',
  description: 'Beheer uw werkschema en beschikbaarheid',
};